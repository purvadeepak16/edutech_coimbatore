# Copy this file to .env and fill the values. Do NOT commit the real .env or service account JSON.
# Either set FIREBASE_SERVICE_ACCOUNT_PATH to the local JSON file path
# or set FIREBASE_SERVICE_ACCOUNT (a JSON string) with the service account contents.

PORT=5000

# Firebase
# Option A (recommended): path to your local service account JSON (ignored by git)
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccount.json

# Option B: service account JSON string (for CI secrets, set in secret manager)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# Your Firebase project DB URL (optional, used by RTDB or other services)
FIREBASE_DATABASE_URL=
